// dbConfig.js

const dbConfig = {
    user: 'your_user',
    host: 'localhost',
    database: 'your_db',
    password: 'your_password',
    port: 5432, // PostgreSQL server port
};

module.exports = dbConfig;
